org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# AGP 9 enables built-in Kotlin by default. For this small MVP we explicitly
# opt out so the project can use the current Kotlin 2.3.21 Compose compiler plugin pair.
android.builtInKotlin=false
